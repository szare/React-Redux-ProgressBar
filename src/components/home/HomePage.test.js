import expect from 'expect';
import React from 'react';
import {mount, shallow} from 'enzyme';
import HomePage from './HomePage';


describe('HomePage Test', () => {
  it('renders div, Header, MainPage and Footer', () => {
  });

});
